<h1><%= @list.name %></h1>

<h1>Tasks</h1>
  <% @tasks.each do |task| %>
  <li>
    <%= task.name %>
    <%= link_to "Remove", method: :delete, data: { confirm: "Are you sure you want to remove this task?"}  %>
  </li>
  <% end %>
