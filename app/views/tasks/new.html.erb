<h1>Create a new task</h1>
<%= simple_form_for [@list, @task] do |t| %>
  <%= t.label :name %>
  <%= t.text_field :name %>

  <%= t.submit %>
<% end %>

<p><%= link_to "Back to list", list_path([:id]) %></p>d
